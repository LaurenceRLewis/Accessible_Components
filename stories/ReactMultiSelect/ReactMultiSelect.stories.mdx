import { Meta } from '@storybook/addon-docs/blocks';
import ReactMultiSelect from '../ReactMultiSelect/ReactMultiSelect';

<Meta title="Components/ReactMultiSelect" component={ReactMultiSelect} />

# ReactMultiSelect

This is a custom multi-select component.

It takes a `selectionModel` prop which can be either `default` or `alternative`. 

// ... more documentation ...
